<?php
/**
 * 
 * Ukasky BBS [http://bbs.ukasky.com]
 * @author Mr. Chen
 * My Blog: http://chen.disyo.com
 */

if(!defined('IN_DISCUZ') && !defined('IN_ADMINCP')) {
	exit('Access Denied');
}

//更新数据库
$tablename = DB::table(ukasky_payforum);

$sql = <<<EOF

DROP TABLE IF EXISTS `$tablename`;

CREATE TABLE `$tablename` (
  `id` int(10) NOT NULL AUTO_INCREMENT,
  `uid` mediumint(8) DEFAULT NULL,
  `username` char(15) DEFAULT '',
  `fid` mediumint(8) DEFAULT NULL,
  `paynum` mediumint(8) DEFAULT NULL,
  `paycredit` char(10) DEFAULT NULL,
  `validdays` int(10) DEFAULT '0',
  `dateline` int(10) DEFAULT NULL,
  `dateend` int(10) DEFAULT NULL,
  PRIMARY KEY (`id`)
) ENGINE=MyISAM;

EOF;

runquery($sql);
$finish = TRUE;
?>